const { expect } = require('chai');

describe('Index', () => {
  it.skip('should create on instance of app', () => {
  });

  it.skip('should call createServer with instance of app', () => {
  });

  it.skip('should call listen with port', () => {
  });
});
